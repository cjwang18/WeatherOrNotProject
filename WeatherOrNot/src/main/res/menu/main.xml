<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity" >

    <!-- Search, should appear as action button -->
    <item android:id="@+id/action_search"
        android:icon="@drawable/ic_action_search"
        android:title="@string/action_search"
        android:showAsAction="ifRoom|collapseActionView"
        android:actionViewClass="android.widget.SearchView" />

    <item
        android:id="@+id/action_tempUnitSelect"
        android:title="@string/action_tempUnitSelect_default"
        android:showAsAction="ifRoom" >
        <menu>
            <item
                android:id="@+id/fUnitSelect"
                android:title="&#xb0;F" />
            <item
                android:id="@+id/cUnitSelect"
                android:title="&#xb0;C" />
        </menu>
    </item>

    <item
        android:id="@+id/action_share_to_facebook"
        android:icon="@drawable/ic_action_share"
        android:title="@string/action_share_to_facebook"
        android:showAsAction="ifRoom" />

    <!-- Settings, should always be in the overflow-->
    <!--<item android:id="@+id/action_settings"
        android:title="@string/action_settings"
        android:orderInCategory="100"
        android:showAsAction="never" />-->

</menu>
